### 1주차

이번에 실제로 실행한 명령과 역할

이번에 설치한 패키지등, 설치관련 한것들을 알려줘

이번에 생성된 파일,폴더 목록 알려줘


현재 프로젝트 기준으로 폴더까지 트리로 보면 이렇게 보면 됩니다.

```
cur_gem_mongo_front_260509/
├── .gitignore
├── 1.md
├── package.json
├── package-lock.json
├── src/
│   ├── app.js
│   └── server.js
└── node_modules/          ← npm install 시 자동 생성 (내부는 패키지 파일 다수)
```

- **직접 만든 폴더**: `src`  
- **npm이 만든 폴더**: `node_modules`

